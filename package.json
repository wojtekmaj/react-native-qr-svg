{
  "name": "@wojtekmaj/react-native-qr-svg-monorepo",
  "version": "1.0.0",
  "description": "@wojtekmaj/react-native-qr-svg monorepo",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @wojtekmaj/react-native-qr-svg build",
    "dev": "yarn workspace @wojtekmaj/react-native-qr-svg watch",
    "lint": "yarn workspaces foreach run lint",
    "postinstall": "husky install",
    "prettier": "yarn workspaces foreach run prettier",
    "test": "yarn workspaces foreach run test",
    "tsc": "yarn workspaces foreach run tsc",
    "unit": "yarn workspaces foreach run unit"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^14.0.0",
    "prettier": "^2.7.0"
  },
  "packageManager": "yarn@3.1.0"
}
